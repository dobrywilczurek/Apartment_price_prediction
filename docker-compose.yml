version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    networks:
      - app-network
    environment:
      - MODEL_READY=1

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge